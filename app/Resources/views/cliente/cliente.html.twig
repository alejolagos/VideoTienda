<!DOCTYPE html>
<html lang="es">
<head>
  <title>VideoTienda</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
</head>
<body>
    <div class="panel panel-default">
        <div class="panel-heading"><h3><img src="{{ asset('movie.ico') }}" class="img-circle" alt="imageMovie" width="40" height="40">Tienda:<small> Perfil Cliente </small></h3>
        </div>
        
        <div class="col-sm-6" style="background-color:lavender;">
            
            <div class="panel-body">
                {#Pintando la tabla#}
                <div class="table-responsive">
                    <table class="table table-striped table-bordered">
                        <p align="center">DISPONIBLES</p>
                        <tr>
                            <th>TITULO</th>
                            <th>GENERO</th>
                            <th>ESTADO</th>
                            <th>DESCRIPCION</th>
                            <th>ACCION</th>
                        </tr>
                        <tr>
                        {%for movie in movies%}
                            <td>{{movie.nombre}}</td>
                            <td>{{movie.genero}}</td>
                            <td>{{movie.estado}}</td>
                            <td>{{movie.descripcion}}</td>
                            <td>
                                <a href="/cliente/accion/{{movie.id}}" class="btn btn-success" role="button">Alquilar</a>
                            </td>
                        </tr>
                        {%endfor%}
                    </table>
                </div>
            </div>
                    <div class="table-responsive">
                        <p align="center">ALQUILADAS</p>
                        <table class="table table-striped table-bordered">
                            <tr>
                                <th>TITULO</th>
                                <th>GENERO</th>
                                <th>ESTADO</th>
                                <th>DESCRIPCION</th>
                                <th># Veces Alquilada</th>
                                <th>ACCION</th>
                            </tr>
                            <tr>
                            {%for movie2 in movies2%}
                                <td>{{movie2.nombre}}</td>
                                <td>{{movie2.genero}}</td>
                                <td>{{movie2.estado}}</td>
                                <td>{{movie2.descripcion}}</td>
                                <td>{{movie2.conta}}</td>
                                <td>
                                    <a href="/cliente/accion/{{movie2.id}}" class="btn btn-info" role="button">Devolver</a>
                                </td>
                            </tr>
                            {%endfor%}
                        </table>
                    </div>
                        <div class="panel-heading"><h5>Opciones de Usuario</h5>
                            <ul>
                                <a href="/logout" class="btn btn-danger" role="button">- Cerrar Sesion</a>
                            </ul>
                        </div>
        </div>      
        <div class="col-sm-6" style="background-color:lavenderblush;">
            <div class="panel-body">
                {#Pintando la tabla#}
                <div class="table-responsive">
                    <form action="/buscar/movie" method="post" align="center">
                            <input type="text" name="buscar">
                            <input type="submit" value="BUSCAR">
                    </form>
                    <table class="table table-striped table-bordered">
                        <br><p align="center">RESULTADO DE LA BUSQUEDA</p>
                        <tr>
                            <th>TITULO</th>
                            <th>GENERO</th>
                            <th>ESTADO</th>
                            <th>DESCRIPCION</th>
                            <th>ACCION</th>
                        </tr>
                        <tr>
                        {%for movie3 in movies3%}
                            <td>{{movie3.nombre}}</td>
                            <td>{{movie3.genero}}</td>
                            <td>{{movie3.estado}}</td>
                            <td>{{movie3.descripcion}}</td>
                            <td>
                                <a href="/cliente/accion/{{movie3.id}}" class="btn btn-success" role="button">Alquilar</a>
                            </td>
                        </tr>
                        {%endfor%}
                    </table>
                </div>
            </div>
                    <h5>NOTA: La busqueda se realiza en peliculas disponibles </h5>
        </div>
    </div>
</body>
</html>
